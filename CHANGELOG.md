# Changelog

All notable changes to this project are documented here.

## [0.1.0] - 2026-09-12

### Added
- `-h`/`--help` and `-v`/`--version` flags.
- LICENSE (MIT) and CONTRIBUTING.md.
- Unit tests for `.gitignore` pattern matching in `scanner.zig`.
- Doc comments on public API in `file_watcher.zig`, `scanner.zig`, `process.zig`.

### Fixed
- README install instructions used a placeholder repository URL.
